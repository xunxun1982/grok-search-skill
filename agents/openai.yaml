interface:
  display_name: "Web Research Direct"
  short_description: "Direct web search and URL fetching"
  default_prompt: "Use $web-research-direct to research a current topic with cited sources."

policy:
  allow_implicit_invocation: true
